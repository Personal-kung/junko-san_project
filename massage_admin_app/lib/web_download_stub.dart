// lib/web_download_stub.dart
void downloadWebFile(String csvData, String fileName) {
  // This remains empty. It's only here so the mobile compiler 
  // has a function signature to look at without crashing.
}